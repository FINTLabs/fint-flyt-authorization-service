apiVersion: fintlabs.no/v1alpha1
kind: Application
metadata:
  name: fint-flyt-authorization-service
  namespace: fintlabs-no
  labels:
    app.kubernetes.io/instance: fint-flyt-authorization-service_fintlabs_no
    app.kubernetes.io/name: fint-flyt-authorization-service
    app.kubernetes.io/version: latest
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: fint-flyt
    fintlabs.no/team: flyt
    fintlabs.no/org-id: fintlabs.no
spec:
  port: 8080
  imagePullPolicy: Always
  orgId: fintlabs.no
  image: ghcr.io/fintlabs/fint-flyt-authorization-service:sha-0000000
  kafka:
    enabled: true
    acls:
      - permission: admin
        topic: 'no-permission'
  database:
    database: fint-flyt
  url:
    hostname: flyt.vigoiks.no
    basePath: ""
  ingress:
    enabled: true
    basePath: path
    middlewares:
      - fint-flyt-auth-forward-sso
  env:
    - name: JAVA_TOOL_OPTIONS
      value: '-XX:+ExitOnOutOfMemoryError -Xmx768M -Xms512M'
    - name: fint.flyt.vigo.sso.client-id
      valueFrom:
        secretKeyRef:
          name: fint-flyt-vigo-oauth2-client
          key: fint.flyt.vigo.sso.client-id
    - name: fint.flyt.altinn.sso.client-id
      valueFrom:
        secretKeyRef:
          name: fint-flyt-altinn-oauth2-client
          key: fint.flyt.altinn.sso.client-id
    - name: fint.flyt.egrunnerverv.sso.client-id
      valueFrom:
        secretKeyRef:
          name: fint-flyt-egrunnerverv-oauth2-client
          key: fint.flyt.egrunnerverv.sso.client-id
    - name: fint.flyt.hmsreg.sso.client-id
      valueFrom:
        secretKeyRef:
          name: fint-flyt-hmsreg-oauth2-client
          key: fint.flyt.hmsreg.sso.client-id
    - name: fint.flyt.authorization.sso.client-id
      valueFrom:
        secretKeyRef:
          name: fint-flyt-authorization-oauth2-client
          key: fint.sso.client-id
  resources:
    limits:
      memory: "1024Mi"
      cpu: "500m"
    requests:
      memory: "512Mi"
      cpu: "150m"
  probes:
    startup:
      path: /actuator/health
      port: 8080
      initialDelaySeconds: 70
      failureThreshold: 12
      periodSeconds: 10
      timeoutSeconds: 3
    readiness:
      path: /actuator/health/readiness
      port: 8080
      initialDelaySeconds: 0
      failureThreshold: 5
      periodSeconds: 10
      timeoutSeconds: 3
    liveness:
      path: /actuator/health/liveness
      port: 8080
      initialDelaySeconds: 0
      failureThreshold: 5
      periodSeconds: 10
      timeoutSeconds: 3
  observability:
    metrics:
      enabled: true
      port: "8080"
      path: /actuator/prometheus
  restartPolicy: Always
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxSurge: 1, maxUnavailable: 0 }
