apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: nfk-no

resources:
  - ../../../base
  - side-oauth2-client.yaml

labels:
  - pairs:
      app.kubernetes.io/instance: fint-flyt-authorization-service_nfk_no
      fintlabs.no/org-id: nfk.no

patches:
  - patch: |-
      - op: replace
        path: "/spec/kafka/acls/0/topic"
        value: "nfk-no.flyt.*"
      - op: replace
        path: "/spec/orgId"
        value: "nfk.no"
      - op: replace
        path: "/spec/url/basePath"
        value: "/nfk-no"
      - op: replace
        path: "/spec/ingress/basePath"
        value: "/nfk-no/api/intern/authorization"
      - op: add
        path: "/spec/env/-"
        value:
          name: "novari.flyt.resource-server.security.api.internal.authorized-org-id-role-pairs-json"
          value: |
            {
              "nfk.no":["USER"],
              "vigo.no":["DEVELOPER","USER"],
              "novari.no":["DEVELOPER","USER"]
            }
      - op: add
        path: "/spec/env/-"
        value:
          name: "fint.flyt.side.available"
          value: "true"
      - op: add
        path: "/spec/envFrom/0"
        value:
          secretRef:
            name: fint-flyt-side-oauth2-client

      - op: add
        path: "/spec/env/-"
        value:
         name: "novari.kafka.topic.orgId"
         value: "nfk-no"
      - op: replace
        path: "/spec/probes/startup/path"
        value: "/nfk-no/actuator/health"
      - op: replace
        path: "/spec/probes/readiness/path"
        value: "/nfk-no/actuator/health/readiness"
      - op: replace
        path: "/spec/probes/liveness/path"
        value: "/nfk-no/actuator/health/liveness"
      - op: replace
        path: "/spec/observability/metrics/path"
        value: "/nfk-no/actuator/prometheus"
    target:
      kind: Application
      name: fint-flyt-authorization-service
